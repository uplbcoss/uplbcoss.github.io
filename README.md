# uplbcoss.github.io

This is just a mirror of http://uplbcoss.ph. Command used:

```sh
$ wget \
      --recursive \
      --no-clobber \
      --page-requisites \
      --convert-links \
      --adjust-extension \
      --restrict-file-names=windows \
      --domains uplbcoss.ph \
      --no-parent \
      uplbcoss.ph
```
